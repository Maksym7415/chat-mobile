import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  text: {
    color: '#ffffff',
    fontSize: 26,
    textAlign: 'center',
    paddingVertical: 8,
    paddingHorizontal: 14,
  },
});
