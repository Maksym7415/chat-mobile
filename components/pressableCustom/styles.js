import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  btn: {
    width: '100%',
    maxWidth: 200,
    marginTop: 16,
  },
  btnText: {
    color: '#ffffff',
    fontSize: 16,
    textAlign: 'center',
    paddingVertical: 8,
    paddingHorizontal: 14,
  },
});
