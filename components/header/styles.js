import {StyleSheet} from 'react-native';
import {theme} from '../../config/theme';

export default StyleSheet.create({
  container: {
    backgroundColor: theme.background_basic_color_header,
    paddingVertical: 5,
    paddingHorizontal: 8,
  },
});
