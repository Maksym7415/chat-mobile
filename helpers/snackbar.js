import Snackbar from 'react-native-snackbar';

export const showSnackbarHelper = ({message}) => {
  // Snackbar.show({
  //   text: message,
  //   // duration: Snackbar.LENGTH_SHORT,
  // });
  console.log(message, 'message');
};
